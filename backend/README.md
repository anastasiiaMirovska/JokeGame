# Backend
First of all go to the appropriate folder by entering:

```cd backend```

## Start:
```npm run start```


## Run tests:
```docker compose -f docker-compose.test.yml up --build --abort-on-container-exit```